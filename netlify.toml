[build]
  command = "npm run build"
  publish = "dist/todo_app/browser"

[build.environment]
  NODE_VERSION = "18"

# Redirection pour le routing Angular
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200